# 参与贡献

1. 不要提交真实额度、令牌、Cookie、私人路径、家庭网络信息或设备标识。
2. 新采集器必须默认关闭，并在文档中说明读取了什么、访问了哪个服务、是否会写入本机文件。
3. 单个采集器失败不能阻止其他卡片和最后一次成功数据继续显示。
4. Kindle 相关修改必须同时提供退出或卸载路径，并列出实际验证过的设备与固件。
5. 提交前运行：

```powershell
npm.cmd run check
```

6. 一个 PR 只解决一个独立问题；采集器、设备安装、天气和部署脚本应尽量拆分，避免互不相关的功能必须一起合并或回滚。
7. 不要在部署脚本中无提示地使用 `git push --force`。确需改写历史时，使用 `--force-with-lease` 并在文档中明确风险。

项目使用 MIT 许可证。引用或改写第三方代码时，必须保留原始许可证和致谢。
